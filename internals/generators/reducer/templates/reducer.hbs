const {{ camelCase name }}Reducer = (state = {}, action) => {
  switch (action.type) {
    // Your cases here
    
    default:
      return state;
  }
};

export default {{ camelCase name }}Reducer;
